<!DOCTYPE html>
<html lang="en" class="h-100">
  <head>
    <%- include('../partials/head'); %>
  </head>

  <body class="body">
    <header class="cabeçalho"><%- include('../partials/header'); %></header>

    <div class="container">
      <div class="tela-inicio">
        <p>Em que podemos ajudar?</p>

        <div class="container">
          <div class="row justify-content-center">
            <div class="row">
              <%if (permissions.edit){%>
              <div class="col">
                <a href="<%=`/cadastro_alianca?ID=${info.link}&page=0` %>">
                  <img src="img/cadastrar.png" height="70%" width="70%" />
                </a>
              </div>
              <%}%> <%if(permissions.view){%>
              <div class="col">
                <a href="<%=`/cadastro_alianca?ID=${info.link}&page=4` %>">
                  <img src="img/pesquisar.png" height="70%" width="70%" />
                </a>
              </div>
              <%}%> <%if(permissions.summary){%>
              <div class="col">
                <a href="/pesquisar">
                  <img src="img/pesquisar.png" height="70%" width="70%" />
                </a>
              </div>
              <%}%>
            </div>
          </div>
        </div>
      </div>
    </div>
    <footer><%- include('../partials/footer'); %></footer>
  </body>
</html>
