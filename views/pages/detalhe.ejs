<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body class="body">
    <header><%- include('../partials/header'); %></header>

    <main class="flex-shrink-0">
      <div class="container">
        <%if (opcao == "Centro") { %> <%- include('../partials/centro',
        {centro:result}); %> <% } %> <%if (opcao == "Trabalhos") { %> <%-
        include('../partials/trabalhos', {trabalhos_centros:result}); %> <% } %>
      </div>
    </main>
    <footer><%- include('.s./partials/footer'); %></footer>
  </body>
</html>
