<% itemId = `${centroId}${question._id}`%>

<textarea
  class="form-control"
  id="<%= itemId%>"
  name="<%= itemId%>"
  rows="3"
  centroId="<%=centroId%>"
  questionId="<%=question._id%>"
  onfocusout="updateFormItem(this)"
>
<%=question.ANSWER%>
</textarea>
